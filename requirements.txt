# Core dependencies for analytics
pandas>=2.0.0
numpy>=1.24.0

# AWS dependencies
boto3>=1.26.0
botocore>=1.29.0
awscli>=1.26.0

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Development dependencies
black>=23.0.0
flake8>=6.0.0 

# Infrastructure as Code
# Note: Terraform is installed separately via package manager
# Install with: winget install HashiCorp.Terraform
# or: choco install terraform (requires admin privileges)
